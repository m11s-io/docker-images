events {}

http {
  include /etc/nginx/mime.types;

  server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location /admin/ {
      try_files $uri $uri/ =404;
    }

    location / {
      return 404;
    }
  }
}
