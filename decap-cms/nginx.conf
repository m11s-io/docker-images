events {}

http {
  include /etc/nginx/mime.types;

  server {
    listen 80;
    server_name _;

    location /admin/ {
      root /usr/share/nginx/html/tenants/$host;
      try_files $uri $uri/ =404;
    }

    location / {
      return 404;
    }
  }
}
